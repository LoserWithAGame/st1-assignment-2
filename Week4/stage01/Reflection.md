# What did you build before using AI?
Before using AI the Human-Written Program had many faults, including a lack of user inputs, little input validation, and no documentation.

# What did AI help you understand?
AI assisted me in understanding the Human-Written code via breaking down the program into a step-by-step order of how it functions. The AI also brought up an aspect I had not considered, which was that all the stored data would be deleted after the program ends.

# Did AI make assumptions?
In the creation of the AI program, the AI assumed the program would continue to run by placing the program in a continuous loop, the AI also assumed

# How did you verify the AI output?
To verify the AI Output I compared the outputs to the inputs, and expected outputs. This works in identifying correct outputs because of the specifications of the project, which is that every appointment needs a Patient Name, Practitioner Name, and Appointment date and time.

# What engineering work remained for you?
After sourcing the program to the AI, the work left for me to complete included understanding, and verifying that the system would function as intended, error testing to ensure the application could manage errors, and implementation of external features (Eg. A database if required, or any provisional/optional features to include.